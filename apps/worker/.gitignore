# Build-time generated by scripts/inject-version.ts.
# We commit a dev stub so local builds/tests resolve the import; the build
# pipeline overwrites this file with real hashes before deploy.
src/_version.ts
# override Task 2's ignore — we commit a dev stub that build-time overwrites
!src/_version.ts
